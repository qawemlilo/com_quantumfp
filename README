Email: qawemlilo@gmail.comThis is a Joomla! 1.6 component that allows an administrator to log in via the front-end and create new user accounts.Once a user has been created, the administrator can upload different types of files for that user.When the user logs in, uploaded files are available for download.